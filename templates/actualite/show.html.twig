{% extends 'base.html.twig' %}

{% block title %}Actualité{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="card">
                <div class="col-lg-12">
                    <img src="{{ 'uploads/actualites/' ~ actualite.image }}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ actualite.titre }}</h5>
                        <h5 class="card-title">{{ actualite.contenu }}</h5>
                        <h5 class="card-title">{{ actualite.date|date('d/m/Y') }}</h5>
                        <a href="#"class="btn btn-primary">Ajouter un commentaire</a>
                    </div>
                </div>                
            </div>
        </div>
    <div>
{% endblock %}
